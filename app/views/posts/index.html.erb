<div>全投稿一覧</div>

<div>本一覧</div>
<% @post_book.each do |post| %>
  <%= link_to book_path(post.id) do %>
    <%= post.body %>
  <% end %>
  <%= post.class %><br>
  <% if user_signed_in? %>
    <% if post.liked_by?(current_user) %>
      <p><span>お気に入り解除: </span><%=link_to post.likes.count, post_likes_path(post.id), method: :delete %></p>
    <% else %>
      <p><span>お気に入り登録: </span><%=link_to post.likes.count, post_likes_path(post.id), method: :post %></p>
    <% end %>
  <% else %>
    <p><span>お気に入り数: </span><%= post.likes.count %></p>
  <% end %>
<% end %>

<div>服一覧</div>
<% @post_clothes.each do |post| %>
  <%= link_to book_path(post.id) do %>
    <%= post.body %>
  <% end %>
  <%= post.class %><br>
  <% if user_signed_in? %>
    <% if post.liked_by?(current_user) %>
      <p><span>お気に入り解除: </span><%=link_to post.likes.count, post_likes_path(post.id), method: :delete %></p>
    <% else %>
      <p><span>お気に入り登録: </span><%=link_to post.likes.count, post_likes_path(post.id), method: :post %></p>
    <% end %>
  <% else %>
    <p><span>お気に入り数: </span><%= post.likes.count %></p>
  <% end %>
<% end %>


<%= link_to "服一覧",clothes_path %>
<%= link_to "本一覧",books_path %>
記事を作ってください
<%= form_with model: @post,method: :post do |f| %>
  <%= f.text_field :body %>
  <%= f.submit "投稿" %>
<% end %>


<% @posts.each do |post| %>
  <%= post.body %>
  <%= post.class %><br>
  <%= post.likes.count %><br>
<% end %>

<%= link_to "トップへ戻る",posts_path %>
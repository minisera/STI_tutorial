  <% @post_list.each do |post| %>
    <% if post.try(:id).nil? %>
      <!-- 新規登録フォーム -->
      <%= form_with posts_path,method: :post do |f|%>
        <%= f.text_field :body %>
        <%= f.submit "投稿"%>
      <% end %>
    <% else %>
      <!-- 登録済み情報の表示 -->
    
    <% end %>
  <% end %>